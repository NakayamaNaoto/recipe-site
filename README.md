# AI Recipe Static Site

AI が生成した料理レシピを紹介する、学習用の静的 Web サイトです。
Google アナリティクス（GA4）のイベント計測やページ遷移分析を行うための教材として作成しています。

本サイトは TypeScript + React + Vite による SPA（Single Page Application）構成で、
データベースは使用せず、recipes 配列からページを生成します。

# 🚀 技術スタック

- React 18
- TypeScript
- Vite
- React Router v6
- Google Analytics（GA4）
- Netlify 対応

# 🌐 サイト構成（ルーティング）

```
/               # レシピ一覧ページ
├── /recipe/:id # レシピ詳細ページ
├── /request    # レシピリクエストフォーム
├── /about      # サイトの説明
└── /contact    # お問い合わせ
```

# 📁 ディレクトリ構成（Vite + React）

```
project-root/
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
└── src/
    ├── main.tsx
    ├── App.tsx
    ├── router/
    │   └── AppRouter.tsx
    ├── pages/
    │   ├── HomePage.tsx            # `/`
    │   ├── RecipeDetailPage.tsx    # `/recipe/:id`
    │   ├── RequestPage.tsx         # `/request`
    │   ├── AboutPage.tsx           # `/about`
    │   └── ContactPage.tsx         # `/contact`
    ├── components/
    │   ├── RecipeCard.tsx
    │   ├── Layout.tsx
    │   └── Header.tsx
    ├── data/
    │   └── recipes.ts              # レシピを静的配列で管理
    └── assets/
        └── images/
```

📦 レシピデータの構造（材料＋手順入り）

レシピはデータベースの代わりに TypeScript 配列 で管理します。

`src/data/recipes.ts`

```ts
export type Recipe = {
    id: string;
    title: string;
    description: string;
    time: string; // 調理時間
    difficulty: string; // 難易度
    ingredients: string[]; // 材料一覧
    steps: string[]; // 調理手順
    image?: string; // 任意：画像パス
};

export const recipes: Recipe[] = [
    {
        id: "1",
        title: "AIが考えたカレー",
        description: "スパイスをほどよく効かせたAIオリジナルカレー。",
        time: "20分",
        difficulty: "かんたん",
        ingredients: ["玉ねぎ 1個", "にんじん 1本", "カレー粉 大さじ2", "水 300ml"],
        steps: ["具材を食べやすい大きさに切る", "鍋で玉ねぎとにんじんを炒める", "水とカレー粉を入れて煮込む", "とろみが出たら完成"],
    },
    {
        id: "2",
        title: "AIが考えたトマトパスタ",
        description: "シンプルで奥深い味わいのトマトパスタ。",
        time: "15分",
        difficulty: "ふつう",
        ingredients: ["パスタ 100g", "トマト缶 1/2缶", "にんにく 1片", "塩・胡椒"],
        steps: ["パスタを茹でる", "フライパンでにんにくを炒める", "トマト缶を加えて煮詰める", "茹でたパスタを絡め、味を調整する"],
    },
];
```

# 📄 ページ描画の仕組み（重要）

## ✔ 一覧ページ /

- recipes.map() ですべてのレシピをカードとして表示
- カードクリックで /recipe/:id へ遷移

## ✔ 詳細ページ /recipe/:id

- useParams() で URL から id を取得
- recipes.find() で該当レシピを取得
- タイトル・説明・材料・手順を表示

イメージ：

```tsx
const { id } = useParams();
const recipe = recipes.find((r) => r.id === id);
```

# 📊 Google アナリティクス（GA4）

## 設置

`index.html` にスクリプトを追加。

```html
<script async src="https://www.googletagmanager.com/gtag/js?id=G-XXXXXXX"></script>
<script src="/ga/gtag.js"></script>
```

## 主な計測イベント

| イベント名            | 内容                 |
| --------------------- | -------------------- |
| `recipe_click`        | レシピカードクリック |
| `scroll_depth`        | スクロール率         |
| `view_ingredients`    | 材料タブ or 材料表示 |
| `view_steps`          | 調理手順を表示       |
| `click_make_it`       | 「作ってみる」ボタン |
| `form_submit`         | フォーム送信         |
| `external_link_click` | 外部リンククリック   |

# 🔧 開発コマンド

## インストール

```bash
npm install
```

## 開発サーバー起動

```bash
npm run dev
```

## ビルド

```bash
npm run build
```

# ☁️ デプロイ（Netlify / Vercel）

## Netlify

`_redirects` を追加（SPA 必須）

```bash
/* /index.html 200
```
